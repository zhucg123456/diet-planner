# 依赖目录（通过 npm install 还原，不入库）
node_modules/

# 构建产物（通过 npm run build 生成）
dist/

# 便携版 Node.js 运行时及安装包
nodejs/
node.zip

# 部署临时脚本（含凭证，禁止上传！）
.vercel-tmp/

# 启动脚本（包含本机路径）
启动服务.bat

# 编辑器/系统文件
.vscode/
.idea/
.DS_Store
*.log
